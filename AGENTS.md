# DogX Web 仓库协作规则

## 操作成功提示

- 表单提交（新增、编辑、分配角色、菜单/接口授权）统一提示“保存成功”（英文 `Saved`）。
- 删除统一提示“删除成功”（英文 `Deleted`）。
- 启用、停用分别提示“启用成功”“停用成功”（英文 `Enabled`、`Disabled`）。
- 重置密码统一提示“密码重置成功”（英文 `Password reset successfully`）。
- 统一复用 `common.*` 公共成功提示翻译，中英文同步维护；不在各页面重复定义，不附加业务对象名称或“需重新登录”等说明。

## 列表数据访问

- 禁止前端列表 N+1 请求：列表加载后的 HTTP 请求次数不得随列表记录数线性增长，禁止在 `for`、`map`、表格行组件、单元格渲染器或逐行 DTO 转换中按记录请求详情、关联数据、权限或状态接口。
- 列表页面渲染所需字段应由列表接口直接返回；多条记录的关联数据使用后端批量接口一次获取，并在前端按 ID 组装。缺少合适接口时应扩展列表响应或新增批量接口，不得使用 `Promise.all(rows.map(...))` 并发掩盖 N+1。
- 用户主动打开单条详情、编辑弹窗或执行单条操作时允许请求对应详情；字典、选项等与记录数量无关的固定次数请求不属于 N+1，但同一页面应复用结果，避免重复加载。
- 确实无法批量处理的场景必须说明原因、设置明确的数量上限并通过评审确认，不得以组件封装或实现方便为由让每个列表行自行发起网络请求。
